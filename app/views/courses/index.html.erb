<p id="notice"><%= notice %></p>

<h1>Courses</h1>
<% if @courses.count > 0 %>
<table frame='box'>
  <thead>
    <tr>
      <th>Course Number</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @courses.order(:course_number).each do |course| %>
      <tr>
        <td><%= course.course_number %></td>
        <td><%= course.description %></td>
        <td><%= link_to 'Show', course %></td>
        <td><%= link_to 'Edit', edit_course_path(course) %></td>
        <td><%= link_to 'Destroy', course, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<% else %>
  <h4>No Courses Exist</h4>
<% end %>
<br>

<%= link_to 'New Course', new_course_path %>

<h2>Bulk Upload Courses</h2>
<%= form_tag import_courses_path, multipart: true do %>
  <%= file_field_tag :file %>
  <%= submit_tag "Import Courses" %>
<% end %>
<h3><a href="courses_template.csv" download>Download Template</a></h3>
<h3><a href="course_test_data.csv" download>Download Example Data</a></h3>